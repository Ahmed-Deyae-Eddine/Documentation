# Questions Fréquentes - Spring

export const Highlight = ({ children, color, url }) => (
  <span
    style={{
      backgroundColor: color,
      borderRadius: '20px',
      marginLeft: '20px',
      color: '#fff',
      padding: '10px',
      cursor: 'pointer',
    }}
    onClick={() => {
      window.open(url, '_blank').focus();
    }}
  >
    {children}
  </span>
);

## 1. C'est quoi Spring ?

Spring est un framework open-source pour le développement des applications Java.

## 2. Quelles sont les avantages d'utiliser Spring ?

## 3. Expliquez le concept d'Inversion de Contrôle (IoC) et d'Injection de Dépendances (DI) dans Spring.

## 4. Qu'est-ce qu'un Bean Spring ?

## 5. C'est quoi la différence entre Spring et Spring Boot ?

## 6. Comment Spring gère-t-il les transactions ?

## 7. Quelle est la différence entre @Transactional au niveau de la méthode et au niveau de la classe ?

## 8. Qu'est-ce que la Programmation Orientée Aspect (AOP) dans Spring ?
